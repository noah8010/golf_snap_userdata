# Flutter 빌드 실행 가이드

## ✅ Flutter 경로와 프로젝트 경로

**Flutter 설치 위치**: `C:\src\flutter\flutter`  
**프로젝트 위치**: `E:\flutter_projects\user_data\golf_stats_app`

### 중요 사항

Flutter는 **PATH 환경 변수에 등록되어 있으면** 프로젝트 위치와 무관하게 어디서든 사용할 수 있습니다. 별도 설정이 필요하지 않습니다.

---

## 🔍 Flutter 환경 확인

다음 명령어로 Flutter가 제대로 설정되어 있는지 확인하세요:

```powershell
# 1. Flutter 버전 확인
flutter --version

# 2. Flutter 경로 확인
where.exe flutter

# 3. Flutter 환경 진단
flutter doctor

# 4. Flutter 환경 상세 진단
flutter doctor -v
```

---

## 🚀 빌드 실행 방법

### 방법 1: 직접 빌드 실행

```powershell
cd e:\flutter_projects\user_data\golf_stats_app

# 빌드 실행
flutter build web --release --base-href /golf_snap_userdata/
```

### 방법 2: 빌드 확인 스크립트 사용

```powershell
cd e:\flutter_projects\user_data\golf_stats_app
.\check_build.ps1
```

### 방법 3: 배포 스크립트 사용 (빌드 + 배포)

```powershell
cd e:\flutter_projects\user_data\golf_stats_app
.\deploy.ps1
```

---

## ✅ 빌드 성공 확인

빌드가 성공하면 다음 파일들이 생성됩니다:

```
build/
└── web/
    ├── index.html          # 웹 앱 진입점
    ├── main.dart.js        # 컴파일된 JavaScript (수 MB)
    ├── flutter.js          # Flutter 런타임
    └── assets/             # 리소스 파일들
        ├── AssetManifest.json
        ├── FontManifest.json
        └── ...
```

### 확인 명령어

```powershell
# 빌드 폴더 확인
Test-Path build\web\index.html

# 빌드 파일 목록 확인
Get-ChildItem build\web -Name

# 주요 파일 크기 확인
Get-Item build\web\main.dart.js | Select-Object Name, @{Name="Size(MB)";Expression={[math]::Round($_.Length/1MB, 2)}}
```

---

## ⚠️ 문제 해결

### Flutter를 찾을 수 없는 경우

```powershell
# PATH에 Flutter 추가 (현재 세션에만 적용)
$env:PATH += ";C:\src\flutter\flutter\bin"

# 또는 영구적으로 추가하려면:
# 시스템 환경 변수에서 PATH에 C:\src\flutter\flutter\bin 추가
```

### 빌드 실패 시

```powershell
# 1. 클린 빌드
flutter clean

# 2. 의존성 재설치
flutter pub get

# 3. 다시 빌드
flutter build web --release --base-href /golf_snap_userdata/
```

### 빌드가 느린 경우

- 첫 빌드는 몇 분 정도 걸릴 수 있습니다
- 이후 빌드는 캐시를 사용하여 더 빠릅니다
- `--verbose` 옵션으로 진행 상황 확인 가능

---

## 📋 빌드 체크리스트

빌드 전 확인:

- [ ] Flutter가 PATH에 등록되어 있음 (`flutter --version` 실행 가능)
- [ ] 프로젝트 디렉토리에 있음
- [ ] 의존성 설치 완료 (`flutter pub get` 실행)
- [ ] 코드에 에러 없음 (`flutter analyze` 실행)

빌드 후 확인:

- [ ] `build/web/index.html` 파일 존재
- [ ] `build/web/main.dart.js` 파일 존재 (수 MB 크기)
- [ ] 빌드 에러 없음

---

## 🎯 다음 단계

빌드가 성공하면:

1. **배포 스크립트 실행**
   ```powershell
   .\deploy.ps1
   ```

2. **또는 수동 배포**
   - gh-pages 브랜치로 전환
   - 빌드된 파일 복사
   - 커밋 및 푸시

---

**참고**: Flutter 설치 위치와 프로젝트 위치가 달라도 문제없습니다. PATH에 등록되어 있으면 어디서든 사용 가능합니다.

