# Flutter 빌드 및 배포 가이드

## ⚠️ 중요: Flutter PATH 설정

Flutter가 `C:\src\flutter\flutter`에 설치되어 있지만 PATH에 등록되지 않은 경우, 다음 방법 중 하나를 사용하세요.

---

## 🔧 방법 1: 스크립트 사용 (권장)

수정된 스크립트들이 자동으로 Flutter 경로를 추가합니다:

```powershell
cd e:\flutter_projects\user_data\golf_stats_app

# 빌드 확인
.\check_build.ps1

# 빌드 및 배포
.\deploy.ps1
```

---

## 🔧 방법 2: 수동으로 PATH 추가

### 현재 세션에만 적용 (임시)

```powershell
# PowerShell에서
$env:PATH = "C:\src\flutter\flutter\bin;" + $env:PATH

# 그 다음 빌드 실행
flutter build web --release --base-href /golf_snap_userdata/
```

### 영구적으로 적용 (시스템 환경 변수)

1. **시스템 속성** 열기
   - `Win + R` → `sysdm.cpl` 입력
   - 또는 제어판 → 시스템 → 고급 시스템 설정

2. **환경 변수** 클릭

3. **시스템 변수**에서 `Path` 선택 → **편집**

4. **새로 만들기** 클릭 → `C:\src\flutter\flutter\bin` 추가

5. **확인** 클릭하여 저장

6. **새 터미널** 열기 (변경사항 적용)

---

## 🔧 방법 3: 전체 경로로 직접 실행

```powershell
cd e:\flutter_projects\user_data\golf_stats_app

# Flutter 전체 경로로 실행
C:\src\flutter\flutter\bin\flutter.bat build web --release --base-href /golf_snap_userdata/
```

---

## ✅ 빌드 확인

빌드가 성공하면 다음 파일들이 생성됩니다:

```
build/
└── web/
    ├── index.html          # 웹 앱 진입점
    ├── main.dart.js        # 컴파일된 JavaScript (수 MB)
    ├── flutter.js          # Flutter 런타임
    └── assets/             # 리소스 파일들
```

### 확인 명령어

```powershell
# 빌드 파일 확인
Test-Path build\web\index.html

# 빌드 파일 목록
Get-ChildItem build\web -Name

# 파일 크기 확인
Get-Item build\web\main.dart.js | Select-Object Name, @{Name="Size(MB)";Expression={[math]::Round($_.Length/1MB, 2)}}
```

---

## 🚀 배포 프로세스

### 자동 배포 (스크립트 사용)

```powershell
.\deploy.ps1
```

이 스크립트는:
1. Flutter 경로 자동 추가 (PATH에 없을 경우)
2. Flutter 웹 빌드 실행
3. gh-pages 브랜치로 전환
4. 빌드된 파일 복사
5. 커밋 및 푸시
6. main 브랜치로 복귀

### 수동 배포

```powershell
# 1. PATH 추가 (필요시)
$env:PATH = "C:\src\flutter\flutter\bin;" + $env:PATH

# 2. 빌드
flutter build web --release --base-href /golf_snap_userdata/

# 3. gh-pages 브랜치로 전환
git checkout gh-pages

# 4. 기존 파일 삭제 (소스 코드 제거)
Get-ChildItem -Path . -Exclude .git | Remove-Item -Recurse -Force

# 5. 빌드된 파일 복사
Copy-Item -Path "build\web\*" -Destination . -Recurse -Force

# 6. 커밋 및 푸시
git add .
git commit -m "Deploy: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')"
git push origin gh-pages

# 7. main 브랜치로 복귀
git checkout main
```

---

## 🐛 문제 해결

### "flutter는 내부 또는 외부 명령이 아닙니다" 에러

**해결 방법:**
1. 스크립트 사용 (자동으로 경로 추가)
2. 또는 수동으로 PATH 추가 (위의 방법 2 참조)

### 빌드가 느린 경우

- 첫 빌드는 몇 분 정도 걸릴 수 있습니다
- 이후 빌드는 캐시를 사용하여 더 빠릅니다

### 빌드 실패 시

```powershell
# 클린 빌드
flutter clean

# 의존성 재설치
flutter pub get

# 다시 빌드
flutter build web --release --base-href /golf_snap_userdata/
```

---

## 📋 체크리스트

빌드 전:
- [ ] Flutter 경로 설정 확인 (스크립트가 자동 처리)
- [ ] 프로젝트 디렉토리에 있음
- [ ] 의존성 설치 완료

빌드 후:
- [ ] `build/web/index.html` 파일 존재
- [ ] `build/web/main.dart.js` 파일 존재
- [ ] 빌드 에러 없음

배포 후:
- [ ] GitHub 저장소의 gh-pages 브랜치 확인
- [ ] 배포 URL에서 앱 작동 확인

---

**참고**: 수정된 스크립트(`deploy.ps1`, `check_build.ps1`)는 Flutter 경로를 자동으로 추가하므로 PATH 설정 없이도 사용할 수 있습니다.

