# GitHub Pages 배포 가이드

## 📋 배포 프로세스

### 전체 흐름
1. **main 브랜치**: 소스 코드 개발 및 커밋
2. **빌드**: Flutter 웹 빌드 실행
3. **gh-pages 브랜치**: 빌드된 파일 복사 및 커밋/푸시
4. **배포 완료**: GitHub Pages가 자동으로 웹사이트 업데이트

---

## 🚀 배포 방법

### 방법 1: 배포 스크립트 사용 (권장)

#### PowerShell
```powershell
cd e:\flutter_projects\user_data\golf_stats_app
.\deploy.ps1
```

#### CMD
```cmd
cd e:\flutter_projects\user_data\golf_stats_app
deploy.bat
```

### 방법 2: 수동 배포

#### 1단계: main 브랜치에서 코드 커밋 (이미 완료했다면 생략)
```powershell
git add .
git commit -m "Update features"
git push origin main
```

#### 2단계: Flutter 웹 빌드
```powershell
flutter build web --release --base-href /golf_snap_userdata/
```

#### 3단계: gh-pages 브랜치로 전환
```powershell
git checkout gh-pages
```
- gh-pages 브랜치가 없으면 생성:
```powershell
git checkout -b gh-pages
```

#### 4단계: 빌드된 파일 복사
```powershell
# 기존 파일 삭제 (.git 제외)
Get-ChildItem -Path . -Exclude .git | Remove-Item -Recurse -Force

# 빌드된 파일 복사
Copy-Item -Path "build\web\*" -Destination . -Recurse -Force
```

#### 5단계: 커밋 및 푸시
```powershell
git add .
git commit -m "Deploy: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')"
git push origin gh-pages
```

#### 6단계: main 브랜치로 복귀
```powershell
git checkout main
```

---

## ✅ 배포 확인

### 1. GitHub 저장소 확인
- https://github.com/noah8010/golf_snap_userdata
- **Settings** → **Pages** → Source: `gh-pages` 브랜치 선택 확인

### 2. 배포 URL 접속
- https://noah8010.github.io/golf_snap_userdata/
- ⏰ GitHub Pages 업데이트에는 **1-2분** 정도 걸릴 수 있습니다.

### 3. 브라우저 캐시 클리어
- 변경사항이 보이지 않으면: **Ctrl + Shift + R** (강력 새로고침)

---

## 📝 배포 체크리스트

- [ ] main 브랜치에 있음
- [ ] 모든 변경사항 커밋됨
- [ ] Flutter 빌드 성공
- [ ] gh-pages 브랜치에 파일 복사됨
- [ ] gh-pages 브랜치에 커밋 및 푸시 완료
- [ ] main 브랜치로 복귀 완료
- [ ] 배포 URL에서 확인 완료

---

## 🔍 주요 명령어 요약

```powershell
# 배포 스크립트 실행
.\deploy.ps1

# 또는 수동 배포
flutter build web --release --base-href /golf_snap_userdata/
git checkout gh-pages
Get-ChildItem -Path . -Exclude .git | Remove-Item -Recurse -Force
Copy-Item -Path "build\web\*" -Destination . -Recurse -Force
git add .
git commit -m "Deploy: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')"
git push origin gh-pages
git checkout main
```

---

**배포 URL**: https://noah8010.github.io/golf_snap_userdata/

