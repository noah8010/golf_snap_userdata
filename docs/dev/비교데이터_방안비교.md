# 비교 통계 데이터 방안 비교 분석

## 📊 현황 분석

### 현재 샘플 데이터
- **파일**: `all_sample_rounds.json`
- **크기**: 6.7MB (227,572줄)
- **user_id**: `noah.nam` (단일 사용자)
- **예상 라운드 수**: 약 30-50개 (추정)

---

## 🔍 두 가지 방안 비교

### 방안 A: 현재 데이터만 사용

#### 장점
✅ **즉시 구현 가능**
- 추가 데이터 생성 불필요
- 개발 시간 단축

✅ **실제 데이터 기반**
- 현실적인 통계 분포
- 일관성 있는 데이터

✅ **간단한 구조**
- 복잡한 데이터 생성 로직 불필요
- 유지보수 용이

#### 단점
❌ **다양성 부족**
- 단일 사용자 데이터
- 비교 대상이 자기 자신의 다른 라운드
- 실제 "다른 사용자와 비교" 느낌 부족

❌ **통계적 의미 약함**
- 30-50개 라운드로는 분포 생성 어려움
- 퍼센타일 계산 정확도 낮음
- 상위/하위 10% 구분 의미 없음

❌ **데모 효과 낮음**
- "나 vs 나"의 비교
- 비교 통계 기능의 가치 전달 어려움

---

### 방안 B: 샘플 데이터 2-3배 확장

#### 장점
✅ **현실적인 비교**
- 다양한 실력 수준의 가상 사용자
- 실제 서비스와 유사한 경험

✅ **통계적 신뢰성**
- 100-150개 라운드로 의미 있는 분포 생성
- 퍼센타일 계산 정확도 향상
- 상위/하위 10% 구분 가능

✅ **데모 효과 높음**
- "나 vs 다른 사용자들" 비교
- 비교 통계 기능의 가치 명확히 전달
- 프로토타입으로서의 완성도 향상

#### 단점
❌ **초기 작업 필요**
- 데이터 생성 스크립트 작성
- 1-2시간 추가 개발 시간

❌ **파일 크기 증가**
- 6.7MB → 약 20MB
- 웹 로딩 시간 약간 증가 (무시 가능)

---

## 💡 추천 방안: **방안 B (샘플 데이터 확장)**

### 이유

1. **프로토타입의 목적**
   - 비교 통계 기능의 **가치를 보여주는 것**이 핵심
   - 방안 A는 기능의 가치를 제대로 전달하지 못함

2. **개발 시간 대비 효과**
   - 1-2시간 투자로 데모 품질 크게 향상
   - 향후 실제 서비스 전환 시에도 동일한 로직 사용 가능

3. **통계적 의미**
   - 30개 라운드: 퍼센타일 의미 없음
   - 100-150개 라운드: 통계적으로 의미 있는 비교 가능

---

## 🛠️ 방안 B 구현 계획

### 1단계: 데이터 변형 전략

현재 라운드 데이터를 기반으로 **통계적으로 변형**하여 다양한 사용자 생성

```python
def create_varied_rounds(base_rounds, multiplier=3):
    """
    기존 라운드를 변형하여 다양한 실력의 가상 사용자 생성
    """
    varied_rounds = []
    
    for i in range(multiplier):
        for round_data in base_rounds:
            # 실력 레벨 설정 (하, 중, 상)
            skill_level = i  # 0: 하, 1: 중, 2: 상
            
            # 새로운 사용자 ID
            new_user_id = f"user_{skill_level}_{uuid.uuid4().hex[:8]}"
            
            # 라운드 데이터 복사 및 변형
            new_round = transform_round(round_data, skill_level, new_user_id)
            varied_rounds.append(new_round)
    
    return varied_rounds
```

### 2단계: 변형 규칙

#### 실력 레벨별 변형

**레벨 0 (하위 실력)**
- 스코어: 원본 + 10~15타
- 페어웨이: 원본 × 0.6
- GIR: 원본 × 0.5
- 퍼팅: 원본 + 4~6개
- 드라이버 거리: 원본 × 0.85

**레벨 1 (중위 실력 - 원본 유지)**
- 스코어: 원본 ± 3타 (랜덤 변동)
- 페어웨이: 원본 ± 10%
- GIR: 원본 ± 10%
- 퍼팅: 원본 ± 2개
- 드라이버 거리: 원본 ± 10m

**레벨 2 (상위 실력)**
- 스코어: 원본 - 8~12타
- 페어웨이: 원본 × 1.3
- GIR: 원본 × 1.5
- 퍼팅: 원본 - 3~5개
- 드라이버 거리: 원본 × 1.15

### 3단계: 구현 스크립트

```python
# docs/dev/expand_sample_data.py

import json
import copy
import random
import uuid
from pathlib import Path

def transform_round(round_data, skill_level, new_user_id):
    """라운드 데이터를 실력 레벨에 따라 변형"""
    new_round = copy.deepcopy(round_data)
    
    # 기본 정보 변경
    new_round['round_id'] = str(uuid.uuid4())
    new_round['user_id'] = new_user_id
    
    # 실력 레벨별 변형 계수
    score_adjust = {0: random.randint(10, 15), 1: random.randint(-3, 3), 2: random.randint(-12, -8)}
    fairway_mult = {0: 0.6, 1: random.uniform(0.9, 1.1), 2: 1.3}
    gir_mult = {0: 0.5, 1: random.uniform(0.9, 1.1), 2: 1.5}
    putt_adjust = {0: random.randint(4, 6), 1: random.randint(-2, 2), 2: random.randint(-5, -3)}
    
    # 스코어 변형
    new_round['total_score'] += score_adjust[skill_level]
    
    # 페어웨이 변형
    new_fairways = int(new_round['fairways_hit'] * fairway_mult[skill_level])
    new_round['fairways_hit'] = max(0, min(new_fairways, new_round['fairways_attempted']))
    
    # GIR 변형
    new_gir = int(new_round['greens_in_regulation'] * gir_mult[skill_level])
    new_round['greens_in_regulation'] = max(0, min(new_gir, 18))
    
    # 퍼팅 변형
    new_round['total_putts'] = max(18, new_round['total_putts'] + putt_adjust[skill_level])
    
    # 스코어 분포 재계산
    new_round = recalculate_score_distribution(new_round, skill_level)
    
    # 홀별 데이터 변형
    for hole in new_round['holes']:
        hole['hole_score_id'] = str(uuid.uuid4())
        hole['round_id'] = new_round['round_id']
        # 홀 스코어 변형 로직...
    
    # 샷 데이터 변형
    for shot in new_round['shots']:
        shot['shot_id'] = str(uuid.uuid4())
        shot['user_id'] = new_user_id
        
        # 드라이버 샷 변형
        if shot['club_type'] == 'CLUB_D':
            distance_mult = {0: 0.85, 1: random.uniform(0.95, 1.05), 2: 1.15}
            if shot['TOTAL']:
                shot['TOTAL'] *= distance_mult[skill_level]
            if shot['CARRY']:
                shot['CARRY'] *= distance_mult[skill_level]
        
        # 퍼팅 변형
        if shot['is_putt']:
            success_rate = {0: 0.7, 1: 1.0, 2: 1.3}
            if random.random() < success_rate[skill_level]:
                shot['putt_made'] = True
    
    return new_round

def expand_sample_data(input_file, output_file, multiplier=3):
    """샘플 데이터를 확장"""
    print(f"Loading {input_file}...")
    with open(input_file, 'r', encoding='utf-8') as f:
        base_rounds = json.load(f)
    
    print(f"Original rounds: {len(base_rounds)}")
    
    print(f"Creating {multiplier}x varied rounds...")
    expanded_rounds = []
    
    for i in range(multiplier):
        skill_level = i % 3  # 0, 1, 2 순환
        for round_data in base_rounds:
            new_user_id = f"user_lv{skill_level}_{uuid.uuid4().hex[:8]}"
            new_round = transform_round(round_data, skill_level, new_user_id)
            expanded_rounds.append(new_round)
    
    print(f"Expanded rounds: {len(expanded_rounds)}")
    
    print(f"Saving to {output_file}...")
    with open(output_file, 'w', encoding='utf-8') as f:
        json.dump(expanded_rounds, f, ensure_ascii=False, indent=2)
    
    print("Done!")

if __name__ == "__main__":
    input_path = "../../assets/data/all_sample_rounds.json"
    output_path = "../../assets/data/all_sample_rounds_expanded.json"
    
    expand_sample_data(input_path, output_path, multiplier=3)
```

### 4단계: 사용 방법

```bash
# 1. 스크립트 실행
cd docs/dev
python expand_sample_data.py

# 2. 기존 파일 백업
mv ../../assets/data/all_sample_rounds.json ../../assets/data/all_sample_rounds_original.json

# 3. 확장된 파일로 교체
mv ../../assets/data/all_sample_rounds_expanded.json ../../assets/data/all_sample_rounds.json
```

---

## 📊 예상 결과

### 데이터 규모
- **원본**: 30-50 라운드 (1명)
- **확장**: 90-150 라운드 (30-50명)

### 통계 분포 예시
```
스코어 분포:
- 70-80타: 10% (상위 실력)
- 80-90타: 30% (중상위)
- 90-100타: 40% (중위)
- 100-110타: 15% (중하위)
- 110타+: 5% (하위)

페어웨이 적중률:
- 70%+: 10%
- 60-70%: 20%
- 50-60%: 40%
- 40-50%: 20%
- 40% 미만: 10%
```

---

## ✅ 최종 권장사항

**방안 B (샘플 데이터 2-3배 확장)를 강력히 추천합니다.**

### 실행 계획
1. ✅ 데이터 확장 스크립트 작성 (1시간)
2. ✅ 스크립트 실행 및 검증 (30분)
3. ✅ BenchmarkRepository 구현 (이미 계획됨)
4. ✅ UI 통합 (이미 계획됨)

### 기대 효과
- 📈 프로토타입 완성도 **대폭 향상**
- 🎯 비교 통계 기능의 가치 **명확히 전달**
- 💼 클라이언트/투자자 데모에 **즉시 활용 가능**
- 🚀 실제 서비스 전환 시 **동일한 로직 재사용**

---

**작성일**: 2025-11-28  
**버전**: 1.0
