# Golf Stats App 인수인계 문서

## 📋 프로젝트 개요

Golf Stats App은 Flutter로 개발된 골프 통계 분석 애플리케이션입니다. 라운드 데이터를 분석하여 스코어, 퍼팅, 샷 등 다양한 통계를 제공합니다.

## 🚀 실행 방법

### Chrome에서 실행 (권장)

프로젝트 루트에 `run_chrome.bat` 파일이 준비되어 있습니다.

```bash
# 프로젝트 폴더에서 실행
.\run_chrome.bat
```

**참고사항:**
- 현재 릴리즈 모드로 실행됩니다 (디버그 서비스 오류 회피)
- Hot Reload는 사용할 수 없으며, 코드 수정 시 재실행이 필요합니다
- Flutter SDK 경로: `C:\src\flutter\flutter\bin`

### Windows에서 실행

```bash
flutter run -d windows
```

## 🌐 웹 배포 (다른 PC에서 접근)

### 방법 1: 로컬 네트워크 공유 (테스트용)

같은 네트워크의 다른 PC에서 접근할 수 있습니다.

```bash
# 프로젝트 폴더에서 실행
.\build_and_serve.bat
```

**접근 방법:**
1. 스크립트 실행 후 표시되는 IP 주소 확인
2. 다른 PC의 브라우저에서 `http://YOUR_IP:8000` 접속
3. 방화벽에서 포트 8000 허용 필요할 수 있음

**참고:** 스크립트를 종료하면 서버도 중지됩니다.

### 방법 2: GitHub Pages (무료 호스팅)

인터넷 어디서나 접근 가능한 웹사이트로 배포합니다.

```bash
# 1. 웹 빌드 생성
.\build_for_github.bat

# 2. GitHub 저장소 생성 및 푸시
# (스크립트의 안내를 따르세요)
```

**배포 후 접근 주소:**
`https://YOUR_USERNAME.github.io/golf_stats_app/`

### 방법 3: Firebase Hosting (권장)

빠르고 안정적인 무료 호스팅 서비스입니다.

```bash
# Firebase CLI 설치 (최초 1회)
npm install -g firebase-tools

# Firebase 로그인
firebase login

# 프로젝트 초기화
firebase init hosting

# 웹 빌드 생성
flutter build web --release

# 배포
firebase deploy
```

**배포 후 접근 주소:**
`https://YOUR_PROJECT.web.app/`

## 📁 프로젝트 구조

```
golf_stats_app/
├── lib/
│   ├── models/              # 데이터 모델
│   │   ├── round.dart       # 라운드 데이터
│   │   ├── hole.dart        # 홀 데이터
│   │   ├── shot.dart        # 샷 데이터
│   │   ├── code_master.dart # 코드 마스터
│   │   └── ...
│   ├── repositories/        # 데이터 접근 계층
│   │   ├── asset_repository.dart    # 에셋 로딩
│   │   ├── stats_repository.dart    # 통계 계산
│   │   └── putting_repository.dart  # 퍼팅 분석
│   ├── viewmodels/          # 상태 관리 (Riverpod)
│   │   └── providers.dart   # 모든 Provider 정의
│   ├── views/               # UI 화면
│   │   ├── dashboard_screen.dart
│   │   └── widgets/         # 재사용 가능한 위젯
│   ├── utils/
│   │   └── app_constants.dart  # 색상, 스타일 상수
│   └── main.dart            # 앱 진입점
├── assets/
│   └── data/
│       ├── all_sample_rounds.json    # 샘플 라운드 데이터
│       └── code_master_data.json     # 코드 마스터 데이터
├── web/                     # 웹 빌드 설정
├── run_chrome.bat           # Chrome 실행 스크립트
└── README.md                # 프로젝트 설명서
```

## 🔧 기술 스택

- **프레임워크**: Flutter 3.35.2
- **언어**: Dart 3.9.0
- **상태 관리**: Riverpod 2.5.1
- **차트**: fl_chart 0.68.0
- **폰트**: google_fonts 6.2.1
- **국제화**: intl 0.19.0

## 💾 데이터 구조

### Round (라운드)
- `userId`: 사용자 ID
- `playedAt`: 플레이 날짜
- `courseName`: 골프장 이름
- `totalScore`: 총 스코어
- `holes`: 홀 데이터 리스트

### Hole (홀)
- `holeNumber`: 홀 번호 (1-18)
- `par`: 파
- `score`: 스코어
- `shots`: 샷 데이터 리스트

### Shot (샷)
- `shotNumber`: 샷 번호
- `club`: 사용 클럽
- `distance`: 거리
- `result`: 결과 (코드 마스터 참조)

## 📊 주요 기능

### 1. 스코어 분석
- 평균 스코어
- 최고/최저 스코어
- 스코어 추세 그래프
- Par별 평균 스코어
- 홀별 평균 스코어
- 스코어 분포

### 2. 퍼팅 분석
- 거리별 성공률 (0-1m, 1-3m, 3-5m, 5m+)
- 쓰리 퍼트 비율
- 첫 퍼트 성공률
- 평균 퍼트 수

### 3. 라운드 필터링
- 최근 10/20/30 라운드
- 전체 기록

## 🔍 주요 파일 설명

### `lib/repositories/stats_repository.dart`
모든 통계 계산 로직이 포함되어 있습니다.
- `calculateAverageScore()`: 평균 스코어 계산
- `getBestScoreRecord()`: 최고 스코어 조회
- `getPuttAnalysis()`: 퍼팅 분석 데이터 생성
- 새로운 통계 추가 시 이 파일에 메서드를 추가하세요.

### `lib/viewmodels/providers.dart`
모든 Riverpod Provider가 정의되어 있습니다.
- `filteredRoundsProvider`: 필터링된 라운드 데이터
- `puttAnalysisProvider`: 퍼팅 분석 데이터
- `scoreTrendProvider`: 스코어 추세 데이터

### `lib/utils/app_constants.dart`
앱 전역 색상 및 스타일 상수가 정의되어 있습니다.
- UI 커스터마이징 시 이 파일을 수정하세요.

## 🛠 개발 가이드

### 새로운 통계 추가하기

1. **StatsRepository에 메서드 추가**
   ```dart
   // lib/repositories/stats_repository.dart
   double calculateNewStat(List<Round> rounds) {
     // 계산 로직
   }
   ```

2. **Provider 추가**
   ```dart
   // lib/viewmodels/providers.dart
   final newStatProvider = Provider<AsyncValue<double>>((ref) {
     final roundsAsync = ref.watch(filteredRoundsProvider);
     final statsRepo = ref.watch(statsRepositoryProvider);
     return roundsAsync.whenData((rounds) {
       return statsRepo.calculateNewStat(rounds);
     });
   });
   ```

3. **UI에서 사용**
   ```dart
   final newStat = ref.watch(newStatProvider);
   ```

### 샘플 데이터 생성

```bash
# 프로젝트 루트에서 실행
python generate_sample_data.py
```

- `SCENARIOS` 변수를 수정하여 플레이어 이름 및 라운드 수 변경 가능
- 생성된 데이터는 `assets/data/all_sample_rounds.json`에 저장됨

## ⚠️ 알려진 이슈

### Chrome 디버그 모드 오류
- **증상**: `Error: Unsupported operation: Cannot send Null`
- **원인**: Flutter 디버그 서비스 도구 오류
- **해결**: 릴리즈 모드로 실행 (`run_chrome.bat` 사용)
- **제한**: Hot Reload 불가, 코드 수정 시 재실행 필요

## 📞 문의사항

프로젝트 관련 문의사항이 있으시면 개발자에게 연락해주세요.

---

**마지막 업데이트**: 2025-11-24
**버전**: 1.0.0
