# 배포 상태 확인 결과

## ⚠️ 확인 필요 사항

터미널 출력이 보이지 않아 배포 상태를 자동으로 확인할 수 없습니다. 다음을 수동으로 확인해주세요:

## 📋 배포 상태 확인 체크리스트

### 1. 빌드 파일 확인
```powershell
# PowerShell에서 실행
Test-Path build\web\index.html
```
- ✅ `True` → 빌드 완료
- ❌ `False` → 빌드 필요

### 2. Git 브랜치 확인
```powershell
git branch --show-current
```
- `main` 브랜치에 있어야 함

### 3. gh-pages 브랜치 확인
```powershell
git branch -a | Select-String "gh-pages"
```
- gh-pages 브랜치가 있는지 확인

### 4. gh-pages 브랜치 파일 확인
```powershell
git checkout gh-pages
Get-ChildItem -Name | Select-Object -First 10
git checkout main
```
- `index.html` 파일이 있는지 확인

### 5. 원격 저장소 확인
```powershell
git remote -v
```
- 원격 저장소가 설정되어 있는지 확인

### 6. 최근 커밋 확인
```powershell
git log --oneline -5
```
- 최근 배포 커밋이 있는지 확인

## 🚀 배포가 안 되어 있다면

### 배포 스크립트 실행
```powershell
.\deploy.ps1
```

또는

```cmd
.\deploy.bat
```

### 수동 배포
1. **빌드**
   ```powershell
   flutter build web --release --base-href /golf_snap_userdata/
   ```

2. **gh-pages 브랜치로 전환**
   ```powershell
   git checkout gh-pages
   # 또는 없으면 생성
   git checkout -b gh-pages
   ```

3. **파일 복사**
   ```powershell
   Get-ChildItem -Path . -Exclude .git | Remove-Item -Recurse -Force
   Copy-Item -Path "build\web\*" -Destination . -Recurse -Force
   ```

4. **커밋 및 푸시**
   ```powershell
   git add .
   git commit -m "Deploy: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')"
   git push origin gh-pages
   ```

5. **main 브랜치로 복귀**
   ```powershell
   git checkout main
   ```

## ✅ 배포 확인

1. **GitHub 저장소 확인**
   - https://github.com/noah8010/golf_snap_userdata
   - Settings → Pages → gh-pages 브랜치 선택 확인

2. **배포 URL 접속**
   - https://noah8010.github.io/golf_snap_userdata/
   - 1-2분 후 업데이트됨

3. **브라우저 캐시 클리어**
   - Ctrl + Shift + R (강력 새로고침)

## 🔍 문제 해결

### 빌드 실패
```powershell
flutter clean
flutter pub get
flutter build web --release --base-href /golf_snap_userdata/
```

### 푸시 실패
- Git 인증 확인
- 원격 저장소 확인: `git remote -v`
- 수동 푸시: `git push origin gh-pages`

### 배포 후 변경사항 미반영
- GitHub Pages 빌드 대기 (1-2분)
- 브라우저 캐시 클리어
- gh-pages 브랜치 파일 확인

---

**확인 스크립트 실행:**
```powershell
.\check_deploy.ps1
```

